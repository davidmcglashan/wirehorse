bugs
----
- some palette undoes e.g. bold don't retransmit to the canvas (but do to the model)
- mouse leaving the window mid-drag
- update the caret position during drags
- double click in an editor invoked from a rectangle behaves weirdly (doesn't in labels ... ?)
- redo doesn't resequence history properly. You can't move freely backwards and forwards ...

backlog
--------
- breadcrumbs
- tabs
- horizontal rule
- more icons
- coloured icons 
  - either inline the SVGs or stackoverflow suggests masking and change background 
    colour https://stackoverflow.com/questions/24933430/img-src-svg-changing-the-styles-with-css
- icon and label
- text paragraph
- opacity
- export to clipboard as a PNG
  - everything
  - selected items
- groups of components
- insert a component from file e.g. zatpark window, modal window
  - some JSON editing
  - remember recently used 
- drag without selecting first
- locking shapes for selection immunity
- is it quicker/more responsive to use a scroll/pan rather than a translate()?
- selection rectangle on glass and encompassing the max bounds of each selected shape
  - remove the CSS
  - suppose this element would intercept glass mouseevents which might be useful for a 'click on the selection' kind of thing?
- keyboard+drag to quickly add 
  - rectangles with 'r'
  - text with 't'
  - labels with 'l'
- Press Enter with a single selection to invoke the glass editor
- tables
- fancy arrows
- alignment guides
- inset guides
- text parsing e.g. this is *bold*
- option+double-click a shape to recentre and re-zoom the display around it
- proper tracking of min values to stop shapes become zero width/height
- selection drag event to draw a <div>, get its bounds, work out what intersects it
- cut copy paste
- inject version
- inject (c) date

meta
----
- License information
  - https://chr15m.github.io/DoodleCSS/
  - Google fonts
  - SVG Repo - zest interface icons collection
