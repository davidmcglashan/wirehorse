bugs
----
- Enter is re-invoking the editor!
- mouse leaving the window mid-drag
- new rectangles have 'undefined' labels
- invoke editor is not working
- alignment bugs
  - comboboxes report their dimensions strangely
  - checkboxes do not report a width and height
  - radiobuttons do not report a width and height

backlog
--------
- Horizontal and vertical arrange features - space & distribute
- All shapes are going to need default W and H values to be aligned properly
- Select dragger
- Text resizing they don’t have widths which is fine but setting one should be easy
- Text fields wrap as they approach the palette
- See selection box while resizing and after edits etc. 
- New elements in centre of screen
- Cmd B for bold etc. 
- Cmd+R rects to account for scale
- Filter icon
- Up down arrows icon for table sorting
- Table component 
  - Ellipsis
  - Hyperlink
  - Activity status
  - Checkbox rows
- keyboard+drag to quickly add 
  - text with 't'
  - labels with 'l'
- lots of the little strings should be constants
----- v0.4
- export to clipboard as a PNG
  - everything
  - selected items
- switch one component to another - merge the two objects {} and set the type
- Map view component (probably an SVG)
- rectangle with the X through it that can be colour changed
----- v0.5
- groups of components
  - select one, select all - just need groups with their members described in the model
- insert a component from file e.g. zatpark window, modal window
  - some JSON editing
  - remember recently used 
- play with the main dropdown
  - accidental horizontal layout looked cool!
  - light grey panel for the credits and acks
  - include the font and icon meta
- drag without selecting first
- locking shapes for selection immunity
- fancy arrows
  - drawn on the glass to always be on top? 
  - glass is a canvas, not a <div>
  - this might help with the selection do-over
  - arrows woud be added to an arrows collection, not a shapes one?
----- v0.6
- tables
  - csv
  - [ ]/[x] for checkboxes
  - _foo_ for hyperlinks
- vertical rule
- horizontal scrollbar
- alignment guides
- inset guides
- text parsing e.g. this is *bold*, [hyperlink], {#ddd}color{#}
- is it quicker/more responsive to use a scroll/pan rather than a translate()?
- option+double-click a shape to recentre and re-zoom the display around it
- proper tracking of min values to stop shapes become zero width/height
----- v0.7
- selection drag event to draw a <div>, get its bounds, work out what intersects it
- cut copy paste
- icon and label
- inject version
- inject (c) date
- alias a field on the palette to change the label name e.g. {'bg': 'Colour'}

meta
----
- License information
  - https://chr15m.github.io/DoodleCSS/
  - Google fonts
  - SVG Repo - zest interface icons collection
